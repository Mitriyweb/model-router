{
  "type": "function",
  "function": {
    "name": "bash",
    "description": "Run a shell command with the workspace as the current directory. Use for tests, builds, git, npm, and other project commands. Prefer grep, glob, read, write, edit, patch, and delete for workspace file work.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "Shell command to execute."
        }
      },
      "required": ["command"]
    }
  }
}
